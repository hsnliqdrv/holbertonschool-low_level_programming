#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str, *str2;

    str = "0123456789";
    puts_half(str);
    str2 = "012345678";
    puts_half(str2);
    return (0);
}
