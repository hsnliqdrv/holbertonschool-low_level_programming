#include "main.h"

/**
 * _islower - checks lowercaseness
 * @c: character to be checked
 *
 * Return: 1 if c is lower case
 *         else zero
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
